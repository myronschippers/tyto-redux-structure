// TODO: create a CLEAR
const elementListReducer = (state = [], action) => {
    switch (action.type) {
        case 'SET_ELEMENTS':
        default:
            return state;
    }
};

export default elementListReducer;